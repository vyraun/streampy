## generates advices

class Advice(object):
	'Class that serves the advices given an experiment ID'
	
	def __init__(self, experiment=0, features=[]):
		print("Experiment initialized");
		
	def getAdvice(self):
		return(0);
		